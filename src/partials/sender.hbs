<h1>Letters from {{tag}}</h1>
<ul>
  {{#each pagination.files}}
	 <li>{{dateFormat date "DD/MM/YYYY"}} {{recipient}}</li>
  {{/each}}
</ul>
{{#if pagination.previous}}
  <a href="{{pagination.previous.path}}">Previous</a>
{{/if}}
{{#if pagination.next}}
  <a href="{{pagination.next.path}}">Next</a>
{{/if}}
